_dev/
isc_template_report.pdf
# Stray PDFs from `typst compile src/<x>.typ` with no output path — never commit
# these; the real example artifacts live in examples/ (see scripts/pack note).
src/*.pdf
src/fonts/modern-isc-fonts
lib/**/*.pdf
scripts/tmp
tmp/
test

# Ignore macOS system files
.DS_Store
.AppleDouble
.Spotlight-V100
.Trashes
._*

# Ignore direnv folder
.direnv/
